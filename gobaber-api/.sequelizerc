const { resolve } = require('path');

module.exports = {
   config: resolve(__dirname, 'src', 'config', 'database.js'), //caminho de configurações do database,
   'models-path': resolve(__dirname, 'src', 'app', 'models'), //caminho da pasta dos models
   'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), //caminho da pasta dos migrations
   'seeders-path': resolve(__dirname, 'src', 'app', 'seeds'), //caminho da pasta dos seeders 
};
